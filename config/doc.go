// Package config загружает и валидирует runtime-конфигурацию из переменных окружения.
//
// Пакет инкапсулирует значения по умолчанию, fallback-логику для legacy ENV
// и fail-fast проверки, которые должны отработать до старта транспорта.
package config
